# claude-notify configuration
# Copy to ~/.config/claude-notify/config and customize

# Your unique ntfy topic (generated during install)
CLAUDE_NOTIFY_TOPIC="claude-notify-CHANGEME"

# ntfy server URL (default: public ntfy.sh)
CLAUDE_NOTIFY_SERVER="https://ntfy.sh"

# Notification priorities per event type
# Values: min, low, default, high, urgent
CLAUDE_NOTIFY_PRIORITY_PERMISSION="high"
CLAUDE_NOTIFY_PRIORITY_IDLE="high"
CLAUDE_NOTIFY_PRIORITY_DONE="default"

# Also show local OS notifications (terminal-notifier on macOS, BurntToast on Windows)
CLAUDE_NOTIFY_LOCAL="true"

# Remote control server URL (for clickable action buttons in notifications)
# Set this to your Cloudflare Tunnel / Tailscale / ngrok URL
# Leave empty to use localhost (action buttons won't work from phone)
CLAUDE_NOTIFY_REMOTE_URL="https://claude-notify.yourdomain.com"
